version: "3.8"

services:
  frontend:
    build:
      context: ./front_despacho
      dockerfile: Dockerfile
    container_name: innovatech_frontend
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
    restart: always
